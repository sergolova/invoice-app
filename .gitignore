# OS files
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.Directory

# Environment
.env
.env.*
!.env.example

# Logs
*.log

# Node.js (root-level, if any)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# PHP / Composer (root-level, if any)
vendor/

# Docker
docker-compose.override.yml
.docker/volumes/

# Build artifacts
dist/
build/
*.tsbuildinfo

# Laravel internal placeholders (keep empty dirs, but don't track each .gitignore)
backend/bootstrap/cache/.gitignore
backend/database/.gitignore
backend/storage/*/.gitignore
backend/storage/*/*/.gitignore
backend/storage/*/*/*/.gitignore

# Tests
.phpunit.result.cache
.phpunit.cache/
.clover
.coverage
.php_cs.cache